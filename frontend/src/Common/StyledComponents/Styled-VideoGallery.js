import styled from "styled-components";

export const VideoGalleryStyled = styled.div`
    .videoGallery .thumb {
        height: 220px;
        width: 100%;
        object-fit: contain;
        cursor: pointer;
    }
`